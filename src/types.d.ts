type MyComponentProps = {
  age: number;
};

type ContextType = {
  age: number;
};

type age = number;
type username = string;
type password = string;

type signUpForm = {
  firstName: string;
  lastName: string;
  age: number;
  password: string;
};
